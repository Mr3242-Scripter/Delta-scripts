local success, Rayfield = pcall(function()
    return loadstring(game:HttpGet("https://sirius.menu/rayfield"))()
end)

if not success or not Rayfield then
    warn("Rayfield failed to load")
    return
end

---------------------------------------------------
-- WINDOW
---------------------------------------------------
local Window = Rayfield:CreateWindow({
    Name = "QuantumX Hub",
    LoadingTitle = "Loading scripts...",
    LoadingSubtitle = "Follow me on TikTok Mr_3242",
    ConfigurationSaving = { Enabled = false }
})

---------------------------------------------------
-- CONFIG
---------------------------------------------------
local GITHUB_API = "https://api.github.com/repos/Mr3242-Scripter/Delta-scripts/contents/Mr_3242%20Pastes?ref=main"
local GITHUB_RAW_BASE = "https://raw.githubusercontent.com/Mr3242-Scripter/Delta-scripts/main/Mr_3242%20Pastes/"

---------------------------------------------------
-- NOTIFY
---------------------------------------------------
local function Notify(t, c)
    pcall(function()
        Rayfield:Notify({
            Title = tostring(t),
            Content = tostring(c),
            Duration = 3,
            Image = 4483362458
        })
    end)
end

---------------------------------------------------
-- BLACKLIST
---------------------------------------------------
local blacklist = {
    "quiz bot script (the question about how to custom quiz here’s the answer this isn’t the script)",
    "fake loading progress",
    "⚠️this is not a script ⚠️ daycare 💥 virus combinations",
    "QuantumX Script list.txt",
    "quantumx hub v2.0!",
    "this isn’t a script its a ui library i made",
    "how to use loadstring",
    "rayfield made by sirius"
}

local function isBlocked(name)
    name = tostring(name or ""):lower()
    for i = 1, #blacklist do
        local bad = tostring(blacklist[i] or ""):lower()
        if bad ~= "" and name:find(bad, 1, true) then
            return true
        end
    end
    return false
end

---------------------------------------------------
-- CATEGORY SETUP
---------------------------------------------------
local categoryOrder = {
    "Admin",
    "Hub",
    "Exploits",
    "Tools",
    "Games",
    "Misc"
}

local categories = {}
local tabs = {}

for _, name in ipairs(categoryOrder) do
    categories[name] = {}
end

---------------------------------------------------
-- HTTP
---------------------------------------------------
local function httpGet(url, retries)
    retries = retries or 3
    for i = 1, retries do
        local ok, res = pcall(function()
            return game:HttpGet(url)
        end)
        if ok and type(res) == "string" and #res > 0 then
            return res
        end
        task.wait(0.25 * i)
    end
    return nil
end

---------------------------------------------------
-- CLEAN TEXT
---------------------------------------------------
local function clean(text)
    return (text or "")
        :gsub("&amp;", "&")
        :gsub("&quot;", '"')
        :gsub("&#39;", "'")
        :gsub("<.->", "")
        :gsub("%s+", " ")
        :match("^%s*(.-)%s*$")
end

---------------------------------------------------
-- URL ENCODE
---------------------------------------------------
local function urlEncode(str)
    return tostring(str)
        :gsub("([^%w%-_%.~])", function(c)
            return string.format("%%%02X", string.byte(c))
        end)
end

---------------------------------------------------
-- CATEGORY DETECTION
---------------------------------------------------
local function getCategory(name)
    local n = name:lower()
    if n:find("admin", 1, true) then
        return "Admin"
    elseif n:find("hub", 1, true) then
        return "Hub"
    elseif n:find("fly", 1, true) or n:find("esp", 1, true) or n:find("teleport", 1, true) then
        return "Exploits"
    elseif n:find("tool", 1, true) or n:find("gui", 1, true) then
        return "Tools"
    elseif n:find("game", 1, true) or n:find("obby", 1, true) or n:find("simulator", 1, true) then
        return "Games"
    end
    return "Misc"
end

---------------------------------------------------
-- STORAGE
---------------------------------------------------
local seen = {}

local function addScript(name, url)
    name = clean(name)
    if name == "" or isBlocked(name) or seen[url] then
        return
    end

    seen[url] = true
    local cat = getCategory(name)

    table.insert(categories[cat], {
        name = name,
        url = url
    })
end

---------------------------------------------------
-- PARSE GITHUB API
---------------------------------------------------
local function parseGitHub(data)
    for name in data:gmatch('"name"%s*:%s*"([^"]+%.txt)"') do
        name = name:gsub("\\u0027", "'")
        name = name:gsub('\\"', '"')

        local displayName = name:gsub("%.txt$", "")
        local encodedName = urlEncode(name)
        local url = GITHUB_RAW_BASE .. encodedName

        addScript(displayName, url)
    end
end

---------------------------------------------------
-- RUN SCRIPT
---------------------------------------------------
local function runScript(url)
    Notify("Loading", "Downloading script...")

    local content = httpGet(url, 3)

    if not content or #content < 20 then
        Notify("Error", "Failed to load script")
        return
    end

    local lower = content:lower()

    if lower:find("<html", 1, true) or lower:find("<!doctype", 1, true) then
        Notify("Error", "GitHub returned an invalid file")
        return
    end

    pcall(function()
        Rayfield:Destroy()
    end)

    task.wait(0.1)

    local ok, err = pcall(function()
        local fn = loadstring(content)
        if not fn then
            error("loadstring failed")
        end
        fn()
    end)

    if not ok then
        Notify("Execution Error", tostring(err or "Unknown error"))
    end
end

---------------------------------------------------
-- BUILD UI
---------------------------------------------------
local function buildUI()
    for _, catName in ipairs(categoryOrder) do
        local list = categories[catName]

        if #list > 0 then
            table.sort(list, function(a, b)
                return a.name:lower() < b.name:lower()
            end)

            local tab = Window:CreateTab(catName, 4483362458)
            tabs[catName] = tab

            for _, script in ipairs(list) do
                tab:CreateButton({
                    Name = script.name,
                    Callback = function()
                        runScript(script.url)
                    end
                })
            end
        end
    end
end

---------------------------------------------------
-- LOAD EVERYTHING
---------------------------------------------------
task.spawn(function()
    local data = httpGet(GITHUB_API, 3)

    if not data then
        Notify("Error", "Failed to access GitHub")
    else
        parseGitHub(data)
    end

    buildUI()

    ---------------------------------------------------
    -- CREDITS
    ---------------------------------------------------
    local CreditsTab = Window:CreateTab("Credits", 4483362458)

    CreditsTab:CreateParagraph({
        Title = "Credits",
        Content = "QuantumX Hub created by Mr_3242\nThanks for using the hub!"
    })

    local function copy(txt)
        if typeof(setclipboard) == "function" then
            pcall(function()
                setclipboard(txt)
            end)
        end
    end

    CreditsTab:CreateButton({
        Name = "Copy TikTok",
        Callback = function()
            copy("https://www.tiktok.com/@Mr_3242")
        end
    })

    CreditsTab:CreateButton({
        Name = "Copy YouTube",
        Callback = function()
            copy("https://www.youtube.com/@Mr_3242.")
        end
    })

    CreditsTab:CreateButton({
        Name = "Copy Twitch",
        Callback = function()
            copy("https://m.twitch.tv/quantumx_42/home")
        end
    })

    ---------------------------------------------------
    -- UNLOAD
    ---------------------------------------------------
    local UnloadTab = Window:CreateTab("Unload", 4483362458)

    UnloadTab:CreateButton({
        Name = "Unload Script",
        Callback = function()
            pcall(function()
                Rayfield:Destroy()
            end)
        end
    })
end)
